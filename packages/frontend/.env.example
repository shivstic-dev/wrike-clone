# API Backend URL
VITE_API_URL=https://wrike-clone-backend.onrender.com/api/v1

# Supabase Realtime (client-side subscriber)
# Copy from Supabase project -> Settings -> API. When both are set, task
# updates arrive via websocket broadcast (~50ms). When omitted, the app
# falls back to visibility-aware polling automatically.
VITE_SUPABASE_URL=https://lsjeobyrmxiqewehhjai.supabase.co
VITE_SUPABASE_ANON_KEY=copy-from-supabase-project-api-settings

# App Configuration
VITE_APP_NAME=Wrike Clone
VITE_APP_VERSION=0.1.0
