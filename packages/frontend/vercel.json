{
  "buildCommand": "cd ../.. && npm install && npm run build -w @wrike-clone/shared && npm run build -w @wrike-clone/frontend",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install --workspace-root --include-workspace-root",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
