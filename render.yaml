services:
  - type: web
    name: wrike-clone-backend
    runtime: node
    region: singapore
    plan: free
    branch: main
    rootDir: .
    buildCommand: npm install && npm run build -w @wrike-clone/shared && npm run build -w @wrike-clone/backend
    startCommand: cd packages/backend && npm run start:prod
    healthCheckPath: /api/v1/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      # Render injects its own PORT automatically — do not override it.
      - key: API_PREFIX
        value: api/v1
      - key: DATABASE_URL
        sync: false
      - key: DB_SSL
        value: "true"
      - key: APP_PUBLIC_URL
        sync: false
      - key: ALLOW_PUBLIC_REGISTRATION
        value: "false"
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      # Your Vercel frontend origin(s), comma-separated. Wildcards are
      # rejected in production by validateProductionConfig.
      - key: CORS_ORIGINS
        sync: false
      # ── Supabase storage + realtime broadcast ──
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SUPABASE_STORAGE_BUCKET
        value: work-management-files
      - key: REALTIME_BROADCAST_ENABLED
        value: "true"
      # ── Optional integrations ──
      - key: SENTRY_DSN
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
